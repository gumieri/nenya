// Package version provides build-time metadata stamped at compile time
// via ldflags, including the semantic version, git commit hash, and
// build timestamp. These are used for diagnostics and /healthz responses.
package version
