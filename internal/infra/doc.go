// Package infra provides infrastructure primitives used across the gateway:
// Prometheus-compatible metrics, HTTP observation middleware, per-model usage
// statistics, rate limiting, response caching, cost tracking, latency sampling,
// and thought-signature deduplication.
package infra
