// Package config defines the configuration types, loading logic, validation,
// secrets management, and engine reference resolution for the Nenya gateway.
// Configuration is loaded from JSON files merged with built-in provider defaults.
package config
